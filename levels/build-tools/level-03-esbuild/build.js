const esbuild = require('esbuild');

// TODO: 完成 ESBuild 配置
// 1. 设置入口文件为 src/index.ts
// 2. 设置输出目录为 dist
// 3. 启用代码压缩
// 4. 启用 sourcemap

esbuild.build({
  // 在这里完成配置
}).catch(() => process.exit(1));